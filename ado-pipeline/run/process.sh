# This directory handles the tasks related to ongoing operations after the app is deployed and running in a live environment.

# The run stage runs the app in the execution environment,
# by launching some set of the app’s processes against a selected release
